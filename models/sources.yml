version: 2

sources:
  - name: salesforce
    database: DBT_DEMO
    schema: SALESFORCE

    tables:
      - name: customers
        config:
          loaded_at_field: updated_at
          freshness:
            warn_after:
              count: 1
              period: hour
            error_after:
              count: 2
              period: hour